PORT=3002
SOLANA_RPC=https://api.mainnet-beta.solana.com

# Run scripts/createToken.ts once to generate these values
ARENA_MINT_ADDRESS=
TREASURY_SECRET_KEY=

# Leave blank in dev — token payments are skipped when ARENA_MINT_ADDRESS is unset

DEV_WALLET_ADDRESS=<your Solana wallet public key — receives house cut + store revenue>
